---
title: Unit analysis
layout: home
parent: Flying
nav_order: 20
---

Standard/regional:

hoppip -> skiploom -> jumpluff:
pidgey (regional) -> pidgeotto (regional) -> pidgeot (regional):
spearow -> fearow:
zubat -> golbat -> crobat:
starly (regional) -> staravia (regional) -> staraptor (regional):
pidove (regional) -> tranquill (regional) -> unfezant (regional):
rufflet (regional) -> braviary (regional):
dratini -> dragonair -> dragonite:
torchic -> combusken -> blaziken:
taillow -> swellow:
rookidee (regional) -> corvisquire (regional) -> corviknight (regional):
doduo (regional) -> dodrio (regional):
fletchling -> fletchinder -> talonflame:

Add picks:

Uncommon:
venonat -> venomoth:
hoothoot -> noctowl:
ledyba -> ledian:
woobat -> swoobat:
archen -> archeops:
noibat -> noivern:

Rare:
natu -> xatu:
yanma -> yanmega:
drifloon -> drifblim:
vullaby -> mandibuzz:

Epic:
murkrow -> honchkrow:
gligar -> gliscor:
wingull -> pelipper:
wattrel -> kilowattrel:

Hatches:
rowlet:

Uniques:

farfetch_d:

galarian_farfetch_d:

scyther:

aerodactyl:

dunsparce -> dudunsparce:

delibird:

skarmory:

volbeat:

illumise:

tropius:

chatot:

mantyke -> mantine:

rotom_fan:

sigilyph:

emolga:

hawlucha:

minior -> minior_kernel_red -> minior_kernel_blue -> minior_kernel_orange -> minior_kernel_green:

minior -> minior_kernel_red -> minior_kernel_blue -> minior_kernel_orange -> minior_kernel_green:

minior -> minior_kernel_red -> minior_kernel_blue -> minior_kernel_orange -> minior_kernel_green:

minior -> minior_kernel_red -> minior_kernel_blue -> minior_kernel_orange -> minior_kernel_green:

minior -> minior_kernel_red -> minior_kernel_blue -> minior_kernel_orange -> minior_kernel_green:

cramorant:


Legendaries:

articuno:

galarian_articuno (regional):

zapdos:

galarian_zapdos (regional):

moltres:

galarian_moltres (regional):

lugia:

ho_oh:

rayquaza:

origin_giratina:

tornadus:

thundurus:

landorus:

yveltal:

celesteela:

enamorus:


